spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cine_db2?createDatabaseIfNotExist=true
    username: root
    password: Davidguetta
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      enabled: true

server:
  port: 8080
  tomcat:
    max-swallow-size: 10MB
    connection-timeout: 5m

jwt:
  secret: "vDveM71iSL+EDBsck6+RCtkt/jh9beKGpLruCBFaMfIB2YphNXBoY86naZUuyoc+75lRpVTUkJ1fipne04+MYA=="
  expiration: 86400000  # 24 horas en milisegundos

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    show-common-extensions: true
    show-extensions: true
  use-fqn: true

file:
  upload-dir: uploads